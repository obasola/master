/*
 * Created on 24 Oct 2015 ( Time 23:20:17 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */
package com.kumasi.journal.test.persistence;


import com.kumasi.journal.domain.jpa.BookCategoryEntity;
import com.kumasi.journal.mock.BookCategoryEntityMock;
import com.kumasi.journal.persistence.PersistenceServiceProvider;
import com.kumasi.journal.persistence.services.BookCategoryPersistence;

import org.junit.Assert;
import org.junit.Test;

/**
 * JUnit test case for BookCategory persistence service
 * 
 * @author Telosys Tools Generator
 *
 */
public class BookCategoryPersistenceTest 
{
	@Test
	public void test1() {
		
		System.out.println("Test count ..." );
		
		BookCategoryPersistence service = PersistenceServiceProvider.getService(BookCategoryPersistence.class);
		System.out.println("CountAll = " + service.countAll() );
	}
	
	@Test
	public void test2() {
		
		System.out.println("Test BookCategory persistence : delete + load ..." );
		
		BookCategoryPersistence service = PersistenceServiceProvider.getService(BookCategoryPersistence.class);
		
		BookCategoryEntityMock mock = new BookCategoryEntityMock();
		
		// TODO : set primary key values here 
		process( service, mock, 0 , 0  );
		// process( service, mock, ... );
	}

	private void process(BookCategoryPersistence service, BookCategoryEntityMock mock, Integer bookId, Integer categoryId ) {
		System.out.println("----- "  );
		System.out.println(" . load : " );
		BookCategoryEntity entity = service.load( bookId, categoryId );
		if ( entity != null ) {
			// Found 
			System.out.println("   FOUND : " + entity );
			
			// Save (update) with the same values to avoid database integrity errors  
			System.out.println(" . save : " + entity );
			service.save(entity);
			System.out.println("   saved : " + entity );
		}
		else {
			// Not found 
			System.out.println("   NOT FOUND" );
			// Create a new instance 
			entity = mock.createInstance( bookId, categoryId ) ;
			Assert.assertNotNull(entity);

			/* NB : this entity is a "Join Table" 
			System.out.println(" . insert : " + entity );
			service.insert(entity);
			System.out.println("   inserted : " + entity );
			*/

			System.out.println(" . delete : " );
			boolean deleted = service.delete( bookId, categoryId );
			System.out.println("   deleted = " + deleted);
		}		
	}
}
